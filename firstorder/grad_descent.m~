function x_opt = grad_descent(f, x0, g)
%
% Simple implementation of gradient descent
%
% Author: Varun Ramakrishna.

if(nargin < 1)
    test_grad_descent;
elseif(nargin < 3)
    gradtype = 'num';
end

while err < tol 
    
    % Compute gradient
    [fx, gx] = f(x(1), x(2));
    
    
    % Line search
    t = fminbnd(
    
    % Update
    x = x + t*gx;
end
